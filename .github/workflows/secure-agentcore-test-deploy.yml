name: Secure AgentCore Test — Terraform CI/CD Deprecated

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "This workflow is deprecated."
          echo "Use test-terraform-stack.yml for Terraform test operations."
