name: Deprecated - Facade Runtime Activate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Use consolidated workflow
        run: |
          echo "This workflow is deprecated."
          echo "Use: Actions -> Test Application Deploy."
          echo "For full deployment, select deploy_mode=full."
          echo "For Runtime only, select deploy_mode=runtime-only."
          exit 1
