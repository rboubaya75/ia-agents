# Security tests

Squelette de tests pour une PR ultérieure.

Tests cibles :

- rejet de `actorId` fourni par le client ;
- rejet de `userId` fourni par le client ;
- JWT invalide ;
- isolation utilisateur ;
- logs sans données sensibles.
